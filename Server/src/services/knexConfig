const { DB_HOST, DB_PORT, DB_USER, DB_NAME } = process.env;

module.exports = require("knex")({
  client: "mysql",
  connection: {
    host: DB_HOST,
    port: DB_PORT,
    user: DB_USER,
    database: DB_NAME,
  },
});
